#!/usr/bin/env python

import argparse

import common

from osbuild import clean

parser = argparse.ArgumentParser()
parser.add_argument("--new-files", action="store_true",
                    help="remove also new files")
args = parser.parse_args()

common.setup()

clean.clean(new_files=args.new_files)
